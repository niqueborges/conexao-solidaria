app: description
service: conexao-solidaria

provider:
  name: aws
  runtime: python3.x

functions:
  hello:
    handler: api/health.health
    events:
      - httpApi:
          path: /
          method: get
